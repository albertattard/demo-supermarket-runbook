<!-- Generated by Sociable Weaver (https://github.com/albertattard/sw). Manual edits will be overwritten when regenerated. -->

   ```shell
   codex exec \
     --ephemeral \
     --sandbox read-only \
     --output-last-message '../codex/codex-review-issue-readiness-issue-004.md' \
     - <<'EOF' > '../codex/codex-review-issue-readiness-issue-004.log' 2>&1
   Use $review-issue-readiness to assess whether this issue is ready to implement: https://github.com/albertattard/demo-supermarket/issues/4
   EOF
   ```

Note that the issue is incomplete and requires some grooming before moving forward as indicated in the response.  

```markdown
1. I can’t access the GitHub issue content, so its requirements cannot be assessed.

Please paste issue #4’s title, description, and comments here.
```

## Introduce implementation agents and Git worktrees

Introduce a second skill that implements a ready issue and explain how Git worktrees isolate parallel work. For issue 4, assign each implementation agent a distinct brief or evaluation focus; asking several agents to solve an identical prompt would mostly produce redundant candidates.

TODO: Add the implementation skill, worktree setup, distinct agent briefs, and one branch or pull request per candidate implementation.

## Review competing implementations of issue 4

Introduce an independent review skill that examines every candidate in its isolated worktree. It must compare the implementations against issue 4's acceptance criteria, automated-test coverage, maintainability, and regressions, then recommend exactly one candidate for merge.

TODO: Add the review skill, review commands, selected-candidate merge, and cleanup of unselected worktrees and branches.
